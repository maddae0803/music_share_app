<% provide(:title, "Edit Profile") %>
<% provide(:button_text, "Save Changes") %>

<h1 class="center"> Update Profile </h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    	<%= render 'form' %>
    	<%= link_to "Delete My Account", @user, method: :delete, data: { confirm: "You sure you wanna do that hun?"}, class: "btn btn-danger" %>
  </div>
</div>